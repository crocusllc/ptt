# ===========================================
# PTT Application Configuration
# ===========================================
# Copy this file to .env and update values before deployment
#
# Generate secure secrets with:
#   openssl rand -base64 32

# Domain Configuration
DOMAIN=localhost

# TLS Configuration
# For development (self-signed): TLS_MODE=internal
# For production (Let's Encrypt): TLS_MODE=you@example.com
TLS_MODE=internal

# Database Configuration
PG_USER=postgres
PG_PASSWORD=<generate-secure-password>
PG_DB=ptt_db
PG_PORT=5432

# Application Security
SECRET_KEY=<generate-secure-key>
AUTH_SECRET=<generate-secure-key>

# Frontend (auto-derived from DOMAIN)
NEXTAUTH_URL=https://${DOMAIN}
AUTH_TRUST_HOST=https://${DOMAIN}
NEXT_PUBLIC_API_URL=https://${DOMAIN}/db
API_URL=https://${DOMAIN}/db
